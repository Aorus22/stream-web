services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: torrent-stream-web-dev
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    ports:
      - "5173:5173"
